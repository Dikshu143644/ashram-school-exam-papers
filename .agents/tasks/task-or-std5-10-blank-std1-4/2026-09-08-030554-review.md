# Mark-neutral किंवा/OR internal-choice pairs for Std 5-10, plus blanked इयत्ता field for Std 1-4 (v1)

Six internal-choice question pairs were added, one to each of Std 5-10, so a student answers either an existing question (A) or a PR#2-sourced alternative (B) carrying the same marks. The existing short-answer question keeps its text and marks tag but gets relabeled `Q.N (A)`; an `or-divider` and a new `Q.N (B)` q-title follow, then all three share the single pre-existing answer-space. Separately, the इयत्ता (class) value in the Std 1-4 student-info box was blanked to a fill line while the label stays, matching the other blank fields; Std 5-10 इयत्ता values remain printed. The change is confined to `index.html` and reuses CSS classes (`or-divider`, `info-line`) already defined on `main`.

Watch for: nothing blocking. One informational note (confirmed): the (B) text is topically and lexically sourced from PR#2 for the same standard but was reformatted into the target file's bilingual house style — Devanagari transliterations and an English `q-en` span were added on top of the PR#2 Marathi core, so it is not a byte-for-byte copy.

**Verdict**: APPROVED

## High-level view

The OR pairs land in exactly the right places: one pair each in Std 5-10, zero in Std 1-4. Each pair's (A) and (B) carry an identical `q-marks-tag` (Std 5-9 at `[१ गुण]`, Std 10 at `[३ गुण]`), and because the student answers only one, the counted marks for the affected question are unchanged.

Counting one question per OR pair, every Std 5-10 paper's per-question mark sum matches its `main` value exactly, and none of the section-total, meta-chip (`एकूण गुण: २०`), marks-box (`/ २०`), or tab-subtext signals were touched — all six papers still read 20.

Each converted question is `q-title (A)` → `or-divider` → `q-title (B)` → a single shared `answer-space`; no answer-space was added or removed, so there are no duplicated or orphaned answer areas.

The (B) questions are PR#2-sourced per standard (Std5 Shut Down, Std6 Program/Application, Std7 open-YouTube-steps, Std8 YouTube-watch, Std9 Monitor+Keyboard, Std10 Personalized Learning), adapted to the file's bilingual style rather than copied verbatim; the English technical tokens are intact.

The Std 1-4 change is scoped to the four इयत्ता `<strong>` values only; the `इयत्ता:` label is retained on all ten papers, and no Std 1-4 question, option, image, or mark changed. No MCQ options were reordered and no answers were revealed anywhere.

<details>
<summary>Issues (0)</summary>

No blocking or actionable concerns. One informational note (not an action item): the (B) text is reformatted from PR#2 into the target's bilingual style rather than copied byte-for-byte — intended and consistent with the file.

</details>

<details>
<summary>Details</summary>

### OR pairs are confined to Std 5-10 and target one question each

The six `or-divider` insertions fall at lines 1707, 1962, 2217, 2472, 2696, and 2952, inside `paper-std-5` through `paper-std-10` respectively (article boundaries at 1465/1720/1975/2230/2485/2726). Std 1-4 (`paper-std-1..4`, starting at 835/997/1151/1305) receive no OR markup. Targets are Q.16 for Std 5-8, Q.13 for Std 9, and Q.14 for Std 10, matching the intended sections (विभाग E for Std 5-9, विभाग F for Std 10). (confirmed)

### Mark-neutrality verified against the base

Within each pair, (A) and (B) carry the same `q-marks-tag`: Std 5-9 both `[१ गुण]`, Std 10 both `[३ गुण]`. Reconstructing each Std 5-10 paper's per-question mark sum while counting only one question per OR pair yields exactly the `main` value for every standard (Std 5-8 → 4, Std 9 → 6, Std 10 → 6 on the per-`q-marks-tag` subset). The diff touches no `sec-marks`, `meta-chip`, `marks-box`, or `tab-subtext` line, and the file still shows `एकूण गुण: २०` ×6 and `/ २०` ×6. (confirmed)

### OR markup shares one answer-space

```
<div class="q-title"><span class="q-no">Q.16 (A)</span> ... [१ गुण]</div>
<div class="or-divider"><span>— किंवा / OR —</span></div>
<div class="q-title"><span class="q-no">Q.16 (B)</span> ... [१ गुण]</div>
<div class="answer-space"> ... </div>   <-- single, shared
```

No `answer-space` was added or removed by the diff, so the pre-existing single answer block is shared across A/B with no duplicated or orphaned answer area. The `or-divider` and `info-line` CSS classes are already defined on `main` (4 and 1 occurrences) and are not redefined here. Div open/close tags balance at 889/889. (confirmed)

### (B) questions are PR#2-sourced per standard, restyled bilingually

Cross-checking `git show c8174d0:index.html`, each (B) matches the same standard's PR#2 question by topic and Marathi phrasing: Std5 "संगणक योग्य पद्धतीने बंद (Shut Down) कसा करावा?" (verbatim match), Std6 Program/Application, Std7 open-YouTube-with-steps, Std8 YouTube-watch, Std9 "Monitor आणि Keyboard यांचे उपयोग स्पष्ट करा", Std10 Personalized Learning (verbatim match). PR#2's source text uses bare English tokens (e.g. "Monitor आणि Keyboard यांचे उपयोग स्पष्ट करा.", "YouTube सारखे application संगणकावर कसे उघडाल? पायऱ्या लिहा."); the inserted versions add Devanagari transliterations `(मॉनिटर)`, `(कीबोर्ड)`, `(यूट्यूब)` and an English `<span class="q-en">` translation to match the target file's house style. All six (B) lines carry a `q-en` span, and English technical tokens (Mouse, Shut Down, Program, Application, YouTube, Monitor, Keyboard, Personalized Learning) are preserved. (confirmed)

### Std 1-4 change is limited to blanking the इयत्ता value

The only Std 1-4 edits are the four `<strong>१ ली/२ री/३ री/४ थी</strong>` → `<span class="info-line"></span>` replacements (lines 856/1018/1172/1326), matching the sibling blank fields. Filtering the diff to exclude the four blanks and the OR-pair lines leaves no other added or removed content. The `इयत्ता:` label survives on all ten papers (grep = 10), all four Std 1-4 printed values are gone (grep = 0), and the six Std 5-10 values remain printed (grep = 6). (confirmed)

### No option reordering or answer disclosure

The diff contains no changes to option/MCQ markup, no answer-key or "correct"/"मॉडेल उत्तर" strings, and every answer-space remains an empty capture. The only (A)-side edit is appending `(A)` to the existing `q-no`; the (A) question's Marathi and English text is byte-identical to `main`. (confirmed)

</details>

<details>
<summary>File map</summary>

- `index.html` — six OR pairs added to Std 5-10 (relabel A / `or-divider` / new B / shared answer-space); four Std 1-4 इयत्ता values blanked to `info-line`. +22 / -10.
- `.agents/tasks/task-or-std5-10-blank-std1-4/` — task scaffolding, FEAT-001 spec, and a per-standard audit report (cross-checked; consistent with findings).

Full diff: `git diff main -- index.html`.

</details>
